# Spring Boot Configuration
spring.application.name=StreamVault API

# Server Configuration - Use Railway's PORT environment variable
server.port=${PORT:8080}

# MongoDB Atlas Configuration
spring.data.mongodb.uri=mongodb+srv://braval5:E1cVluARNzmXZaw2@streamvault.o1obulk.mongodb.net/streamvault-db?retryWrites=true&w=majority

# Database and Collection Names
spring.data.mongodb.database=streamvault-db

# CORS Configuration - Allow production frontend URL
spring.web.cors.allowed-origins=http://localhost:3000,https://digital-video-store-frontend.vercel.app,https://*.vercel.app
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Logging
logging.level.com.bhrugu.api.restapi=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
